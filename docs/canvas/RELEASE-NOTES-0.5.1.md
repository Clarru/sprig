# Sprig v0.5.1

This patch places the React client directive before imports in the portable UI entry point, allowing the v0.5 semantic scene components to compile correctly in Next.js consumers.

All v0.5.0 semantic engine, diagram design, migration, layout, and interaction changes are included unchanged.
